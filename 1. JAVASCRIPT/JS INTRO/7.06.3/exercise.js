let variable = true;
console.log("il valore di variable è :" + variable + " ed è di tipo " + typeof variable);

let variableNumber = Number(variable);
console.log("il valore di variable è :" + variableNumber + " ed è di tipo " + typeof variableNumber);

let variableString = String(variable);
console.log("il valore di variable è :" + variableString + " ed è di tipo " + typeof variableString);

let variableBoolean = Boolean(variable);
console.log("il valore di variable è :" + variableBoolean + " ed è di tipo " + typeof variableBoolean);